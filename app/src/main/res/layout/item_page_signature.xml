<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="?attr/colorSurfaceContainer"
    app:cardCornerRadius="@dimen/radius"
    app:cardElevation="0dp"
    android:layout_marginBottom="@dimen/padding"
    style="?attr/materialCardViewElevatedStyle">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/padding">

        <!-- 顶部：App 图标 + 应用名/Activity -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <io.github.xiaofeidev.round.RoundFrameLayout
                android:layout_width="40dp"
                android:layout_height="40dp"
                app:rd_radius="30dp">

                <ImageView
                    android:id="@+id/appIcon"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:adjustViewBounds="true"
                    android:scaleType="fitXY"
                    android:src="@mipmap/ic_launcher"
                    tools:ignore="ContentDescription" />
            </io.github.xiaofeidev.round.RoundFrameLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginStart="@dimen/padding">

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/appName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    android:textSize="14sp"
                    android:textColor="?colorOnSurface"
                    tools:text="支付宝" />

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/activityName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textSize="12sp"
                    android:textColor="?colorOnSurfaceVariant"
                    tools:text="com.alipay.mobile.bill.BillDetailActivity" />
            </LinearLayout>
        </LinearLayout>

        <!-- 页面结构指纹 -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/structureFingerprint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/one_padding"
            android:ellipsize="end"
            android:maxLines="2"
            android:textSize="11sp"
            android:fontFamily="monospace"
            android:textColor="?colorOnSurfaceVariant"
            tools:text="FrameLayout[LinearLayout[TextView,ImageView],ScrollView]" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
